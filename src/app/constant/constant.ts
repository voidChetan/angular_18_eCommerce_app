export const Constant = {
    LOCAL_KEY:'ecom18'
}

export enum productStatus {
    active = "",
    deActive = 1,
    outOfStock = 2
}

export enum ProductCode {
    active,
    deActive,
    outOfStock
}